required_fields:
  default:
    - summary
    - reporter
    - status
  open:
  stable:
    - stepsToStabilise

incident:
  default:
    template: incident.twig
  review:
    template: review.twig
    widgets:
      - media
incident_list:
  default:
    template: incidents.twig
    widgets: []
